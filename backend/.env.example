# All new behavior is opt-in. The iOS app is NOT connected to this backend.
BACKEND_ENABLED=false
BACKEND_PORT=8000
# Local-only demo identity; never expose this demo server to the internet.
DEMO_AUTH_ENABLED=false
DEMO_TOKEN=
SEED_DEMO_DATA=false
# Independent provider flags. Secrets belong here, never in Swift or source control.
IFM_ENABLED=false
IFM_API_KEY=
ELEVENLABS_ENABLED=false
ELEVENLABS_API_KEY=
